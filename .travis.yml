language: rust
rust:
  - stable
  - beta
  - nightly
script:
  - rm -f target/debug/deps/libserde_xml_rs*.rlib
  - cargo build --verbose
  - cargo test --verbose
cache: cargo
before_cache:
  - cargo clean -p serde-xml-rs
env:
  global:
    - RUST_BACKTRACE=1
matrix:
  fast_finish: true
  allow_failures:
    - rust: nightly
